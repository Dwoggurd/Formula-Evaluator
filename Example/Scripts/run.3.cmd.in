:; if [ -z 0 ]; then
  @echo off
  goto :WINDOWS
fi

: #-----------------------------------------------------------------------------
: # This script can be executed on both:
: # - Windows (cmd)
: # - Linux (shell)
: #-----------------------------------------------------------------------------

: #-----------------------------------------------------------------------------
: # Linux
: #-----------------------------------------------------------------------------
set args='--cfg=Config/cfg.multi-files.txt'
cmd="${CMAKE_BINARY_DIR}/FormulaEvaluator/FormulaEvaluator"
echo --------------------------------------------------------------------------------
echo Args: Multi-files config
echo Starting $cmd "$args" $*
echo --------------------------------------------------------------------------------
$cmd args $*
exit

: #-----------------------------------------------------------------------------
: # Windows
: #-----------------------------------------------------------------------------
:WINDOWS

set args="--cfg=Config/cfg.multi-files.txt"
set cmd="${CMAKE_BINARY_DIR}/FormulaEvaluator/FormulaEvaluator.exe"
echo --------------------------------------------------------------------------------
echo Args: Multi-files config
echo Starting %cmd% %args% %*
echo --------------------------------------------------------------------------------
%cmd% %args% %*
