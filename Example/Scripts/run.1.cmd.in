:; if [ -z 0 ]; then
  @echo off
  goto :WINDOWS
fi

: #-----------------------------------------------------------------------------
: # This script can be executed on both:
: # - Windows (cmd)
: # - Linux (shell)
: #-----------------------------------------------------------------------------

: #-----------------------------------------------------------------------------
: # Linux
: #-----------------------------------------------------------------------------
args='--cfg=Config/cfg.txt'
cmd="${CMAKE_BINARY_DIR}/FormulaEvaluator/FormulaEvaluator"
echo --------------------------------------------------------------------------------
echo Args: Single config file
echo Starting $cmd "$args" $*
echo --------------------------------------------------------------------------------
$cmd $args $*
exit

: #-----------------------------------------------------------------------------
: # Windows
: #-----------------------------------------------------------------------------
:WINDOWS

set args="--cfg=Config/cfg.txt"
set cmd="${CMAKE_BINARY_DIR}/FormulaEvaluator/FormulaEvaluator.exe"
echo --------------------------------------------------------------------------------
echo Args: Single config file
echo Starting %cmd% %args% %*
echo --------------------------------------------------------------------------------
%cmd% %args% %*
